/**
 * DeepSeekGUI brand name for the official sidebar brand slot. The mark
 * itself stays the official whale (2026-09-06 ruling): DeepSeekGUI is a
 * non-commercial open-source set of DSH plugins, not a separate brand.
 */

/**
 * Render the DeepSeekGUI brand name; the shell owns the surrounding
 * typography.
 * @returns the brand name text.
 */
export function DeepSeekGUIBrandName() {
  return <span>DeepSeekGUI</span>
}
