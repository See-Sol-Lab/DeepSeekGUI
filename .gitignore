CLAUDE.local.md
.env
node_modules/
lib/
*.tsbuildinfo
pnpm-debug.log
.pnpm-store/
.cache/
examples/*/*.jsonl
.storages/
.sessions/
examples/*/.sessions/
coverage/
.coverage-times.json
.doc-typecheck-*/
.node-next-types-*/
.oxlint-contract-*/
.oxlintrc.contract-*.json
oxlint-contract-*.ts
.humanize/
tmp/
.claude/commands/
.claude/launch.json
.claude/settings.json
.vscode/
.DS_Store
.idea
mise.toml
dist-exe/
python/sdk-runtime/src/deepseek_harness_runtime/runtime/deepseek-harness-sdk-runtime-*
python/sdk-runtime/src/deepseek_harness_runtime/runtime/node/
python/**/__pycache__/
python/**/.pytest_cache/
packages/**/__pycache__/
apps/web/dist/
dist/
.artifacts/
.dsh-build/
.playwright-mcp/
.orig
.worktrees/
worktrees/
.agents/worktrees/
.typert-*/

# Vendored 上游只提交 TypeScript 源码。tsconfig.base.json 把 @deepseek-ai/*
# 的 paths 指到 vendor/*/src，tsc 因此会就地编译出 .js/.d.ts/.map——那是本地
# 构建产物，不是上游的一部分。范围收在 src/ 下：vendor/cordis/bin.js 是真源码。
/vendor/*/src/**/*.js
/vendor/*/src/**/*.js.map
/vendor/*/src/**/*.d.ts
/vendor/*/src/**/*.d.ts.map

# 例外：DeepSeekGUI 皮肤插件的 lib/ 是**手写产物**，不是编译输出。
# 它的形状（__ModuleLoader__.load 自注册）是官方 client 运行时的契约，
# 没有对应的 TS 源码可以重新生成——被忽略掉就等于插件整个丢失。
!/apps/desktop/theme-plugin/lib/
!/apps/desktop/theme-plugin/lib/*.js

# 同理：目录选择器插件的 lib/ 也是手写产物（P8-D11）。它由 DSH 那个 Node 进程
# 加载，不经 tsdown、也不进 asar；被忽略掉的话，仓库里就只剩一条会 disable 官方
# picker 的 overlay 而没有替代实现——克隆下来构建即失败（shipPickerPlugin 的
# entry 断言会当场拦住，但那已经是浪费了一次构建）。
!/apps/desktop/picker-plugin/lib/
!/apps/desktop/picker-plugin/lib/*.js

# 同理：设置页插件的 lib/ 也是手写产物（P8-D39）。经 --patch overlay 注入官方
# settings.section 槽位，同样没有 TS 源码可重新生成；D29 施工时曾被上面的
# lib/ 规则误伤（当时靠 git add -f 才进的仓库），此例外让后续改动正常入库。
!/apps/desktop/settings-plugin/lib/
!/apps/desktop/settings-plugin/lib/*.js
